/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Enables React's strict mode to help catch potential issues
  swcMinify: true,
  output: "export",
};

module.exports = nextConfig;
